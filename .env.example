# SQLite keeps the whole platform runnable with no database server to install.
# Swap this for a postgresql:// URL and change the datasource provider in
# prisma/schema.prisma when the platform moves to a hosted database.
DATABASE_URL="file:./dev.db"
